# Example local environment file.
# Decap CMS with the `git-gateway` backend does not work by itself on a static export:
# it requires Netlify Identity + Git Gateway or your own self-hosted CMS backend/auth flow.
