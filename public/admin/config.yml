backend:
  name: git-gateway
  repo: TheVeryGit/oriokerg
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/cf4c374d-f26f-4302-82b4-9b07ac3b4b50/pkce
  auth_token_endpoint: /sites/cf4c374d-f26f-4302-82b4-9b07ac3b4b50/token
  gateway_url: https://gateway.decapbridge.com
  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

logo_url: https://decapbridge.com/decapcms-with-bridge.svg
site_url: https://oriokerg.ru

media_folder: public/images/uploads
public_folder: /images/uploads

slug:
  encoding: unicode
  clean_accents: true
  sanitize_replacement: "-"

collections:
  - name: cats
    label: Кошки и производители
    folder: content/cats
    create: true
    identifier_field: name
    slug: "{{slug}}"
    extension: md
    format: frontmatter
    fields:
      - { label: Имя, name: name, widget: string }
      - label: Тип
        name: type
        widget: select
        options:
          - Производитель
          - Котёнок в продаже
      - label: Пол
        name: gender
        widget: select
        options:
          - Кот
          - Кошка
      - { label: Окрас, name: color, widget: string }
      - { label: Цена, name: price, widget: number, required: false }
      - { label: Описание, name: description, widget: text }
      - label: Фотографии
        name: photos
        widget: list
        field: { label: Фото, name: photo, widget: image }
      - { label: Доступно, name: available, widget: boolean, default: true }

  - name: kittens
    label: Котята в продаже
    folder: content/kittens
    create: true
    identifier_field: name
    slug: "{{slug}}"
    extension: md
    format: frontmatter
    fields:
      - { label: Имя, name: name, widget: string }
      - label: Пол
        name: gender
        widget: select
        options:
          - Кот
          - Кошка
      - { label: Окрас, name: color, widget: string }
      - { label: Цена, name: price, widget: number }
      - { label: Описание, name: description, widget: text }
      - label: Фотографии
        name: photos
        widget: list
        field: { label: Фото, name: photo, widget: image }
      - { label: Зарезервировано, name: reserved, widget: boolean, default: false }

  - name: settings
    label: Настройки сайта
    files:
      - label: Контакты и общее
        name: contacts
        file: content/settings/contacts.md
        fields:
          - { name: phone, label: Телефон, widget: string }
          - { name: telegram, label: "Telegram (ссылка)", widget: string }
          - { name: whatsapp, label: "WhatsApp (ссылка)", widget: string }
          - { name: address, label: "Адрес (необязательно)", widget: string, required: false }
      - label: Главная страница
        name: homepage
        file: content/settings/homepage.md
        fields:
          - { name: hero_title, label: "Заголовок hero", widget: string }
          - { name: hero_subtitle, label: "Подзаголовок hero", widget: string }
          - { name: intro_text, label: "Вступительный текст", widget: text }
      - label: О питомнике
        name: about
        file: content/settings/about.md
        fields:
          - { name: title, label: Заголовок, widget: string }
          - { name: body, label: "Текст страницы", widget: markdown }
          - { name: photo, label: Фото, widget: image, required: false }
